const sur = 'Nana'
typeof sur === 'string'

if (!('serviceWorker' in navigator)) {
  console.log('heya')
}

const greeting = 'Hello'
console
  .log(`${greeting} world`)

  [(1, 2, 3)].forEach(x => console.log(x))
